version: '3.8'
services:
  comfy-bridge:
    build: .
    container_name: comfy-bridge
    env_file:
      - .env
    volumes:
      - ./workflows:/app/workflows
    extra_hosts:
      - "host.docker.internal:host-gateway"